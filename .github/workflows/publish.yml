name: Build and publish wheels to PyPI

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

permissions:
  id-token: write # Required for trusted publishing
  contents: read

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, macos-latest, windows-latest ]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install cibuildwheel
        run: python -m pip install --upgrade pip cibuildwheel

      - name: Build wheels
        run: python -m cibuildwheel --output-dir wheelhouse
        env:
          CIBW_BUILD: "cp312-*"
          CIBW_SKIP: "*-musllinux*"
          CIBW_ARCHS_WINDOWS: "AMD64"
          CIBW_BEFORE_ALL_LINUX: |
            set -eux
            PKG_MGR=yum
            if command -v dnf >/dev/null 2>&1; then PKG_MGR=dnf; fi
            $PKG_MGR install -y epel-release
            $PKG_MGR install -y cmake git gcc-c++ make eigen3-devel

            rm -rf ceres-solver
            git clone --depth 1 --recurse-submodules --shallow-submodules https://github.com/ceres-solver/ceres-solver.git
            test -f ceres-solver/third_party/abseil-cpp/CMakeLists.txt
            cmake -S ceres-solver -B ceres-solver/build \
              -DCMAKE_BUILD_TYPE=Release \
              -DCMAKE_INSTALL_PREFIX=/ceres-install \
              -DCMAKE_INSTALL_LIBDIR=lib \
              -DBUILD_TESTING=OFF \
              -DBUILD_EXAMPLES=OFF \
              -DBUILD_BENCHMARKS=OFF \
              -DMINIGLOG=ON \
              -DSUITESPARSE=OFF \
              -DLAPACK=OFF
            cmake --build ceres-solver/build --parallel
            cmake --install ceres-solver/build

            test -f /ceres-install/lib/cmake/Ceres/CeresConfig.cmake
            echo "Installed Ceres config:"
            ls -l /ceres-install/lib/cmake/Ceres/CeresConfig.cmake
          CIBW_ENVIRONMENT_LINUX: "CMAKE_PREFIX_PATH=/ceres-install
            Ceres_DIR=/ceres-install/lib/cmake/Ceres"
          CIBW_BEFORE_ALL_MACOS: |
            rm -rf /tmp/vcpkg
            git clone --depth 1 https://github.com/microsoft/vcpkg.git /tmp/vcpkg
            /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
            /tmp/vcpkg/vcpkg install eigen3 ceres
          CIBW_ENVIRONMENT_MACOS: "CMAKE_TOOLCHAIN_FILE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake"
          CIBW_BEFORE_ALL_WINDOWS: |
            if exist C:\vcpkg rmdir /s /q C:\vcpkg
            git clone --depth 1 https://github.com/microsoft/vcpkg.git C:\vcpkg
            C:\vcpkg\bootstrap-vcpkg.bat -disableMetrics
            C:\vcpkg\vcpkg.exe install --triplet x64-windows eigen3 ceres
          CIBW_ENVIRONMENT_WINDOWS: "CMAKE_PREFIX_PATH=C:/vcpkg/installed/x64-windows
            Eigen3_DIR=C:/vcpkg/installed/x64-windows/share/eigen3
            Ceres_DIR=C:/vcpkg/installed/x64-windows/share/ceres"

      - name: Upload wheels as artifact
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.os }}
          path: wheelhouse/*.whl

  publish:
    name: Publish to PyPI
    needs: build_wheels
    runs-on: ubuntu-latest
    environment:
      name: pypi
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Download wheel artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: wheels-*
          path: dist
          merge-multiple: true
      - name: Publish to PyPI (Trusted Publishing)
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages_dir: dist
